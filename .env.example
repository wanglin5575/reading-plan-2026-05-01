# Firecrawl 抓取（可选，但强烈建议设置以获得最佳抓取效果）
# 申请：https://firecrawl.dev/
FIRECRAWL_API_KEY=fc-xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# SQLite 数据库文件路径（默认在 ./data/reading-plan.db）
# 上线 Vercel 时改为外部数据库（Turso / Supabase 等）
DATABASE_PATH=./data/reading-plan.db
